# 11 — Fases 2, 4, 5 e 3c (implementado)

Só interface — sem tocar em cálculo, banco, rota ou regra de negócio.

## Fase 2 — Mobile
- Indicador "● salvo" volta a ficar visível no drill-in de Lançamentos (antes a
  barra inteira sumia); só a barra de botões some, mantendo o foco na seção.
- Barra de ações do formulário de despesa fixa no rodapé (sticky) no celular.
- Alvos de toque: checkbox 16→20/22px; links de ação pequenos com min-height 40px.

## Fase 4 — Navegação
- Sidebar de IA de 13→4 itens (Central de IA, Copiloto AP, Conhecimento,
  Memória); as demais sub-telas seguem nas pílulas da Central de IA.
- "Visão Geral (IA)" → "Central de IA" (fim do rótulo ambíguo).
- Aba/sidebar de Financeiro alinhadas ao título da tela ("Financeiro / Caixa").

## Fase 5 — Acessibilidade (aria)
- `aria-live="polite"` no indicador de salvamento e no toast; `role="alert"
  aria-live="assertive"` na mensagem de login.
- `aria-expanded` no botão do drawer (☰), alternando true/false.
- `aria-current="page"` no item de navegação ativo (abas + sidebar).

## Fase 3c — Limpeza de dashboards
- Removido o gráfico gêmeo `cCatPie` (doughnut "Composição do custo 2026") no
  Financeiro — redundante com o ranking em barras `cCat` acima, que é mais
  acionável (mostra valores por categoria). Reversível.

## Verificação
- **bun: 127 testes** passam.
- **Varredura headless (20 telas × 4 temas): 0 erros JS, 0 vazamento, 0 overflow
  do corpo.** Aria conferido (live/expanded/current). `cCatPie` sem nenhuma
  referência real (só comentário).
